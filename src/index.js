const md5 = require('md5');
console.log('hello world!');

const fileSystem = {
  userConfig: {
  },
  appData: {

  },
  project: {
    setProjectPath (projectPath) { // 切换项目时 setProjectPath
      // todo
    }
  }
};


module.exports = fileSystem;
